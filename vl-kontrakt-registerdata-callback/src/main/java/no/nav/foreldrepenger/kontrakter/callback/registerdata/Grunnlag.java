package no.nav.foreldrepenger.kontrakter.callback.registerdata;

/**
 * Definerer grunnlagsstrukturen som det gis callback for
 */
public enum Grunnlag {
    /**
     * Grunnlag med informasjon om:
     * I(nntekt): Inntekter relevant for saken
     * A(rbeid): Yrkesaktivitet / Arbeidsavtaler relevant for saken
     * Y(telse): Informasjon om ytelser relevant for saken
     */
    IAY
}
